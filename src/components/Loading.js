import React, { useState, useEffect } from "react";
import "../styles/header.css";

const Loading = () => {
  const content = (
    <React.Fragment>
      <span>Loading...</span>
    </React.Fragment>
  );
  return content;
};

export default Loading;
